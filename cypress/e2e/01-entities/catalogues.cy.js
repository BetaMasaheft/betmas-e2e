describe('Catalogues files page', { tags: '@container' }, () => {
    beforeEach(() => {
        cy.visit('catalogues/list')
    })
    // Todo(DP): what about catalogues ?
    it.skip('', () => {
    })
  })