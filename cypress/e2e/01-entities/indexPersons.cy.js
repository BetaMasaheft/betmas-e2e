describe('Person Index page', { tags: '@container' }, () => {
    beforeEach(() => {
        cy.visit('IndexPersons')
    })

    // TODO: see user.md#Open_record_when_ID_known
    it.skip('', () => {
    })
})